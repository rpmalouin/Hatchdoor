# Copy this file to .env. Every value is optional: Hatchdoor and Docker Compose
# provide the defaults shown below. Uncomment only the values you need to pin.
# NO secrets belong in this sample — tokens and passwords are placeholders here.

# Docker Compose host mounts. These paths are read by Compose on the host.
# HOST_VAULT_PATH=/absolute/path/to/your/markdown-vault
# HOST_CACHE_PATH=./data/cache
# HOST_STATE_PATH=./data/state
# HOST_MODELS_PATH=./models

# Server and storage (environment-only). Docker Compose fixes these container
# paths and the published port; use these only when running Hatchdoor directly.
# VAULT_PATH=./vault
# HATCHDOOR_CACHE_DB=./data/cache/hatchdoor-cache.sqlite3
# HOST=127.0.0.1
# PORT=42824
# HATCHDOOR_SETTINGS_FILE=/path/outside/your/vault/settings.json
# The Vault collection registry. The default is the container path below, which
# does not exist when running Hatchdoor directly; set this to relocate it.
# `just dev-start` points it at .dev/state/vaults.json automatically.
# HATCHDOOR_VAULT_REGISTRY_PATH=/data/state/vaults.json

# --- gdrive build: Google Drive vault over WebDAV (see FORK.md) ---
# The rclone-webdav sidecar (in docker-compose.yml) serves a Google Drive
# remote to Hatchdoor over WebDAV. RCLONE_REMOTE is the rclone remote name
# (created with `rclone config`, token lives in ./rclone/rclone.conf — never
# committed). WEBDAV_USER/WEBDAV_PASS are the credentials the sidecar requires;
# Hatchdoor authenticates to it with the same pair when you register the vault
# as a WebDAV endpoint (url http://rclone-webdav:42825).
# RCLONE_REMOTE=gdrive:MyObsidian
# WEBDAV_USER=webdav
# WEBDAV_PASS=change-me-strong-password

# Web access (environment-only). A Docker first run without this token prints a
# fresh token and the exact assignment to add here. Demo mode is browse-only.
# HATCHDOOR_WEB_BEARER_TOKEN=
# HATCHDOOR_DEMO_MODE=false

# Settings — server-wide note handling. Leave these unset to manage them live
# in Settings. A non-empty .env value pins that one setting until it is removed
# and restarted.
# HATCHDOOR_ARCHIVE_PREFIX=90-archive/
# HATCHDOOR_EMBED_LAYERS=true

# Settings — Agent access (MCP). MCP stays disabled until enabled in Settings;
# it always needs its own bearer token, even for read-only access.
# HATCHDOOR_MCP_ENABLED=false
# HATCHDOOR_MCP_WRITE_ENABLED=false
# HATCHDOOR_MCP_BEARER_TOKEN=
# HATCHDOOR_MCP_ALLOWED_ORIGINS=http://127.0.0.1,http://localhost

# Settings — Uploads. Limits are bytes (10 MiB for web uploads, 5 MiB inline).
# HATCHDOOR_MAX_ATTACHMENT_BYTES=10485760
# HATCHDOOR_MCP_MAX_BASE64_BYTES=5242880

# Versioning is not configured here. Git belongs to a Vault, not to the
# server: add or edit a Vault in Settings, or use the edit_vault MCP tool.
# Upgrading a single-vault deployment that used HATCHDOOR_EXCLUDE or the old
# HATCHDOOR_GIT_* variables? Leave them exactly as they are for one start, so
# Hatchdoor can import them, then delete them: it will not start again until
# you do. They are import-only and are not valid configuration for a registry
# Vault. See docs/migrations/legacy-single-vault.md.

# Logging (environment-only).
# RUST_LOG=hatchdoor=info,tower_http=info,axum::rejection=warn
